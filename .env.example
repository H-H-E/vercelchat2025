# ==============================================================================
# AUTHENTICATION (NextAuth.js)
# ==============================================================================
# The base URL of your application. Required for NextAuth.js, especially in production.
# For development, this is typically http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"

# A random secret string used to hash tokens, sign cookies, and generate cryptographic keys.
# Generate one using: `openssl rand -base64 32` or from https://generate-secret.vercel.app/32
# IMPORTANT: Keep this secret safe!
NEXTAUTH_SECRET="your-very-secure-nextauth-secret"


# ==============================================================================
# DATABASE (PostgreSQL)
# ==============================================================================
# Connection string for your PostgreSQL database.
# Used by Drizzle ORM.
# Example: postgresql://user:password@host:port/database?sslmode=require
POSTGRES_URL="postgresql://your_db_user:your_db_password@your_db_host:5432/your_db_name?sslmode=prefer"


# ==============================================================================
# REDIS (Optional, for resumable streams if configured)
# ==============================================================================
# Connection string for your Redis instance.
# Used for features like resumable server-sent event (SSE) streams.
# Example: redis://user:password@host:port or redis://localhost:6379
REDIS_URL="redis://localhost:6379"


# ==============================================================================
# AI PROVIDERS
# ==============================================================================

# xAI API Key (if using xAI models for chat/image)
# Get your xAI API Key here: https://console.x.ai/
XAI_API_KEY="your-xai-api-key"

# OpenAI API Key (if using OpenAI models, e.g., for embeddings)
# Get your OpenAI API Key here: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key"

# Embedding Model Configuration
# Specifies the model to use for generating embeddings.
# Example: "text-embedding-3-small", "text-embedding-3-large", "text-embedding-ada-002"
EMBEDDING_MODEL="text-embedding-3-small"


# ==============================================================================
# VERCEL SPECIFIC (Optional, if deploying to Vercel)
# ==============================================================================
# The following keys might be automatically configured by Vercel when deploying.

# Vercel Blob Storage Token (if using Vercel Blob for file storage)
# Instructions: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_your-blob-read-write-token"

# Note: POSTGRES_URL and REDIS_URL above can also be Vercel provisioned services.
# Vercel might set these automatically based on your linked Vercel Storage.

# ==============================================================================
# GENERAL NOTES
# =================================================Rproject's .gitignore.
# - For production, always use strong, unique secrets and manage them securely.
# ==============================================================================
